replicaCount: 1

image:
  repository: rmikl/vnc_server
  pullPolicy: IfNotPresent
  tag: 1.0.698

service:
  type: ClusterIP
  httpPort: 80

existingSecret: "vnc-credentials"

vncServer:
  serviceAccount:
    name: vnc-admin

env:
  USER: "rmikl"
  RESOLUTION: "1860x1040"

persistence:
  enabled: true
  storageClass: dlink
  size: 10Gi